const accountId=14563212  //user cant chnages const variable if it assign 
let username="ayush1404"   // let is a scope level variable define as we can easily use same variable at diffrenr scope and their value would be diffrent
var password="aayu1404"   // var can also use but we now limitig the uses as once the var value chnage it effect every where in code
city="mumbai"  // we have define var /let instead on not defining

let name; // when we not define value then console say the value is undefine


username="test10404"
password="test1404"
city="madhubani"


console.table(
    [accountId,username,password,city,name]

);

